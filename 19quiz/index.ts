// 19


var Guests = ["Sasha","Jonson","Dennie"];  

console.log(`We are inviting ${Guests.length} number of guests in dinner.`);

let newGuests = [ 'Johnson', 'Sasha', 'Dani', 'Jonson', 'Maik', 'Roy' ];

console.log(`We are inviting ${newGuests.length} number of guests in dinner.`);

let remGuests = [ 'Johnson', 'Sasha' ];

console.log(`We are inviting ${remGuests.length} number of guests in dinner.`);

let endGuests = [ ];

console.log(`We are inviting ${endGuests.length} number of guests in dinner.`);