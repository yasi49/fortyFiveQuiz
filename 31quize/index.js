var users = ["Smith", "Admin", "John"];
var length = users.length;
if (length === 0) {
    console.log(length, "We need to find some users!");
}
else {
    console.log("All users available.");
}
// "Smith", "Admin", "John"
var users1 = [];
var length = users1.length;
if (length === 0) {
    console.log(length, "We need to find some users!");
}
else {
    console.log("All users available.");
}
